
// This LEG function computes the factorial of 
// a non-negative integer number. It will not
// terminate on negative numbers.

int factorial(int n) {
  if (n == 0) {
    return 1;
  }
  else if (n == 1) {
    return 1;
  }
  else {
    return n * factorial(n - 1);
  }
}
